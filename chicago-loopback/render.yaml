services:
  - type: web
    name: chicago-loopback-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: PYTHONPATH=src python -m uvicorn loopback.main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true